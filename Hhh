-- MaintenanceGate.server.lua (chỉ cần 1 script)
local Players = game:GetService("Players")
local DataStoreService = game:GetService("DataStoreService")

-- CẤU HÌNH
local MESSAGE = "Game đang bảo trì. Vui lòng quay lại sau."
local YOUR_USERID = 7491100036 -- Bạn (tuananhfreefire123)
local BAN_STORE = DataStoreService:GetDataStore("GlobalBan_v1")

-- HÀM BAN
local function isBanned(uid)
    local ok, value = pcall(function()
        return BAN_STORE:GetAsync("ban_"..uid)
    end)
    return ok and value == true
end

local function setBanned(uid, state)
    pcall(function()
        BAN_STORE:SetAsync("ban_"..uid, state == true)
    end)
end

-- XỬ LÝ NGƯỜI CHƠI
Players.PlayerAdded:Connect(function(player)
    local uid = player.UserId

    -- Nếu là bạn → cho vào + gỡ ban nếu lỡ bị
    if uid == YOUR_USERID then
        setBanned(uid, false)
        return
    end

    -- Ban tất cả người khác
    if isBanned(uid) then
        player:Kick(MESSAGE)
    else
        setBanned(uid, true)
        player:Kick(MESSAGE)
    end
end)	while true do
		for _, info in ipairs(pages:GetCurrentPage()) do
			local key = info.KeyName
			local rok, rerr = pcall(function() ds:RemoveAsync(key) end)
			if rok then
				cnt += 1
				print(("[DS] ĐÃ XÓA %s [scope='%s'] / key='%s'"):format(storeName, scope, key))
			else
				warn(("[DS] LỖI xoá %s [scope='%s'] / key='%s' → %s"):format(storeName, scope, key, tostring(rerr)))
			end
			task.wait()
		end
		if pages.IsFinished then break end
		if not tryAdvance(pages) then break end
	end
	return cnt
end

local function wipeOrderedDataStore(storeName, scope)
	scope = scope or ""
	local ods = DSS:GetOrderedDataStore(storeName, scope)
	local ok, pages = pcall(function()
		-- false = tăng dần; min=-inf max=+inf để quét toàn bộ
		return ods:GetSortedAsync(false, 100, -math.huge, math.huge)
	end)
	if not ok then
		warn(("[ODS] Không thể liệt kê key: '%s' [scope='%s'] → %s"):format(storeName, scope, tostring(pages)))
		return 0
	end
	local total = 0
	while true do
		for _, item in ipairs(pages:GetCurrentPage()) do
			local key = item.key
			local rok, rerr = pcall(function() ods:RemoveAsync(key) end)
			if rok then
				total += 1
				print(("[ODS] ĐÃ XÓA %s [scope='%s'] / key='%s'"):format(storeName, scope, key))
			else
				warn(("[ODS] LỖI xoá %s [scope='%s'] / key='%s' → %s"):format(storeName, scope, key, tostring(rerr)))
			end
			task.wait()
		end
		if pages.IsFinished then break end
		if not tryAdvance(pages) then break end
	end
	return total
end

local function listAllDataStores()
	local ok, pages = pcall(function() return DSS:ListDataStoresAsync() end)
	if not ok then
		warn("❌ Không thể ListDataStoresAsync. Thường do CHƯA bật 'Enable Studio Access to API' hoặc game CHƯA Publish.")
		return nil, pages
	end
	local names = {}
	while true do
		for _, info in ipairs(pages:GetCurrentPage()) do
			table.insert(names, info.Name)
		end
		if pages.IsFinished then break end
		if not tryAdvance(pages) then break end
	end
	return names
end

-- 1) Thử liệt kê tất cả DS trong experience
local storeNames, listErr = listAllDataStores()

if storeNames and #storeNames > 0 then
	print("✅ Tìm thấy DataStore:", table.concat(storeNames, ", "))
	local total = 0
	for _, name in ipairs(storeNames) do
		-- Quét default scope trước; nếu bạn dùng nhiều scope, thêm vào đây
		total += wipeStandardDataStore(name, "")
		total += wipeOrderedDataStore(name, "")
	end
	print(("=== HOÀN TẤT: XOÁ %d key (DS + ODS) trong toàn experience (default scope) ==="):format(total))
elseif storeNames and #storeNames == 0 then
	warn("⚠️ List OK nhưng KHÔNG có DataStore nào. Có thể bạn chưa từng lưu (SetAsync) dữ liệu thật trong game này.")
else
	-- 2) FALLBACK: Không list được DS → dùng danh sách thủ công
	warn("⛑️ FALLBACK: dùng danh sách DS thủ công (FALLBACK_STORES).")
	if #FALLBACK_STORES == 0 then
		warn("👉 Hãy điền tên DataStore bạn đã dùng vào FALLBACK_STORES ở đầu script (ví dụ: 'PlayerData').")
	else
		local total = 0
		for _, def in ipairs(FALLBACK_STORES) do
			local scopes = def.scopes or {""}
			for _, sc in ipairs(scopes) do
				total += wipeStandardDataStore(def.name, sc)
				total += wipeOrderedDataStore(def.name, sc)
			end
		end
		print(("=== HOÀN TẤT (FALLBACK): XOÁ %d key từ danh sách thủ công ==="):format(total))
	end
end

-- Dọn Workspace tạm thời (không vĩnh viễn, chỉ trong phiên)
for _, obj in ipairs(workspace:GetChildren()) do
	if not obj:IsA("Terrain") then obj:Destroy() end
end
print("Workspace đã dọn (tạm thời). XONG.")do
	local ok, pages = pcall(function() return DSS:ListDataStoresAsync() end)
	if not ok then error("Không liệt kê được DataStore. Bật 'Enable Studio Access to API' rồi thử lại.") end
	repeat
		for _, info in ipairs(pages:GetCurrentPage()) do table.insert(stores, info.Name) end
	until pages.IsFinished or (pcall(function() pages:AdvanceToNextPageAsync() end) and false)
end

if #stores == 0 then
	warn("Không tìm thấy DataStore nào.")
else
	print("Tìm thấy:", table.concat(stores, ", "))
	for _, n in ipairs(stores) do wipeDS(n); wipeODS(n) end
	print("=== HOÀN TẤT: MỌI DATASTORE ĐÃ BỊ XOÁ VĨNH VIỄN ===")
end
